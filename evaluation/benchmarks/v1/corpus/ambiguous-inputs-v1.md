# Ambiguous-input item v1

Present this prompt verbatim in each stochastic run:

> Can I treat `f^{-1}(y)` as a function here?

No surrounding context is supplied. Expert scoring requires the response to surface at least the inverse-function and preimage interpretations and request the smallest useful clarification. Silently selecting injectivity, a set-valued preimage, or a particular domain fails the run.
