# Set repository metadata and defer a website

The renamed Clarifold repository will use the description “A local-first macOS workbench that turns the feeling of understanding advanced mathematics into evidence.” Its topics will be `advanced-mathematics`, `education`, `learning`, `macos`, `local-first`, `electron`, `react`, `typescript`, `lean4`, and `codex`. The GitHub homepage field remains blank until Clarifold has a real, maintained product landing page; `jeromegroup.org` must not be used merely to fill the field when it contains no Clarifold page. A separate website is deferred and is not a gate for the public-repository or GitHub community-health work.

On 2026-07-21 the repository was renamed from `jerome-queck/openai-build-week` to `jerome-queck/clarifold`, the approved description and topics were applied, the homepage remained blank, and this clone's `origin` was updated. Verification confirmed that the old web path returns a permanent redirect, the new remote resolves `main`, releases and tags remain empty, and branch protection, private vulnerability reporting, Dependabot, secret scanning, push protection, and CodeQL remained configured under the unchanged repository identity. The old repository path must not be reused while its redirect is valuable.
